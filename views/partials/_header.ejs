<style>
  #login-form {
  display: flex;
  flex-direction: row;
  justify-content: space-around;
 }
</style>
<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-item nav-link" href="/urls">My URLs</a>
          <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
          <div id="login-form">
            <% if(user){ %>
              <form method="POST" action="/logout" id="login-form">
              <span> Logged in as: <%=user.email%>
               <button type="submit" class="btn btn-primary">Logout</button></span>
            </form>
               <% } else { %>
                 <a class="btn btn-dark btn-sm mr-2" href="/login">Log In</a>
                 <a class="btn btn-dark btn-sm" href="/register">Register</a>
               <% } %>
          </div>
        </div> 
      </div>
  </nav>
</header>
